import { PrettyDecentEditor } from '../../../../../slate';
declare type CreateTableProps = {
    editor: PrettyDecentEditor;
    cols: number;
    rows: number;
};
export declare const createTable: ({ editor, cols, rows }: CreateTableProps) => void;
export {};
